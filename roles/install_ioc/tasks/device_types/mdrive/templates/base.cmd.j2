
dbLoadDatabase("{{ install_ioc_template_root_path }}/dbd/{{ install_ioc_executable }}.dbd")
{{ install_ioc_executable }}_registerRecordDeviceDriver(pdbbase)

# mdrive specific commands
{% for device in ioc.devices %}
drvAsynIPPortConfigure("{{ device.io_port }}", "{{ device.host }}:{{ ioc.environment.IP_PORT }}")
ImsMDrivePlusCreateController("{{ device.io_port }}_Mtr", "{{ device.io_port }}", "", "100", "1000")
{% endfor %}

dbLoadTemplate("$(TOP)/db/motor.substitutions")
dbLoadTemplate("$(TOP)/db/asynRecord.substitutions")
