---
# Tasks for xspress3 role

- name: Install common startup files
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ install_ioc_ioc_directory }}/iocBoot"
    owner: "{{ softioc_user }}"
    group: "{{ softioc_group }}"
    mode: "0664"
  with_fileglob: "{{ role_path }}/tasks/device_types/{{ ioc.type }}/files/*.cmd"

- name: Install auto_settings file
  ansible.builtin.template:
    src: "{{ role_path }}/tasks/device_types/{{ ioc.type }}/templates/auto_settings.req.j2"
    dest: "{{ install_ioc_as_directory }}/req/auto_settings.req"
    owner: "{{ softioc_user }}"
    group: "{{ softioc_group }}"
    mode: "0664"

- name: Install base startup script
  ansible.builtin.template:
    src: "{{ role_path }}/tasks/device_types/{{ ioc.type }}/templates/base.cmd.j2"
    dest: "{{ install_ioc_ioc_directory }}/iocBoot/base.cmd"
    owner: "{{ softioc_user }}"
    group: "{{ softioc_group }}"
    mode: "0664"

- name: Install postInit.cmd override
  ansible.builtin.template:
    src: "{{ role_path }}/tasks/device_types/{{ ioc.type }}/templates/postInit.cmd.j2"
    dest: "{{ install_ioc_ioc_directory }}/iocBoot/postInit.cmd"
    owner: "{{ softioc_user }}"
    group: "{{ softioc_group }}"
    mode: "0664"
